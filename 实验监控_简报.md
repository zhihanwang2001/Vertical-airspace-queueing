# HCA2C-Wide 实验监控简报
**更新时间**: 2026-01-28 12:05

---

## 当前状态

### 实验进程
- **PID**: 63283
- **CPU使用**: 98.2%
- **运行时间**: 2分钟
- **状态**: 正在训练中

### 修改内容
```
Action Space调整:
  Service intensities: [0.1, 2.0] → [0.3, 1.8]
  Arrival multiplier:  [0.5, 5.0] → [0.7, 4.0]
```

### 时间估算
- 开始: 12:03 (实际)
- 预计完成: 12:39 (~36分钟)
- 剩余时间: ~34分钟

---

## 实验配置

- **Seeds**: 42, 43, 44
- **Load**: 3.0x baseline
- **Timesteps**: 500,000 per seed
- **每个seed时间**: ~12分钟

---

## 预期结果

### 之前（极端）
- Mean: -365
- Crash: 100%

### 预期（调整后）
- Mean: 50K-120K
- Crash: 20-40%
- 仍远低于HCA2C-Full (228,945)

---

## 完成后行动清单

### 1. 检查结果 (5分钟)
```bash
# 查看结果
cat Data/ablation_studies/hca2c_wide/hca2c_wide_seed*.json

# 计算统计
python -c "
import json
import numpy as np
results = []
for seed in [42, 43, 44]:
    with open(f'Data/ablation_studies/hca2c_wide/hca2c_wide_seed{seed}_results.json') as f:
        results.append(json.load(f))
rewards = [r['mean_reward'] for r in results]
crashes = [r['crash_rate'] for r in results]
print(f'Mean: {np.mean(rewards):.1f} ± {np.std(rewards):.1f}')
print(f'Crash rate: {np.mean(crashes)*100:.0f}%')
"
```

### 2. 更新Manuscript

#### 文件1: tables/tab_ablation_simple.tex
```latex
HCA2C-Wide & 821K & [新mean] & [新std] & [新CV] & [新crash%] \\
```

#### 文件2: sections/ablation_study_simple.tex
- Line 12: 更新action space描述
- Line 24: 更新结果数字
- Line 26: 更新分析

#### 文件3: manuscript.tex
- Line 66 (Abstract): 如需要，更新crash rate
- Line 81 (Highlights): 如需要，更新crash rate

### 3. 重新编译
```bash
cd Manuscript/Applied_Soft_Computing/LaTeX
pdflatex manuscript.tex
pdflatex manuscript.tex
```

---

## 监控命令

```bash
# 实时查看进度
watch -n 10 'ls -lht Data/ablation_studies/hca2c_wide/ | head -5'

# 检查进程
ps aux | grep run_ablation_studies | grep -v grep

# 查看日志
tail -f hca2c_wide_rerun.log
```

---

**状态**: 实验运行中，预计12:39完成
